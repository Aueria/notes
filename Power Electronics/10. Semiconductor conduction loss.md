A boost converter is used as an example.

Now, we include the losses from:
1. MOSFET: on-resistance $R_{on}$
2. Diode: Constant forward voltage drop $V_D$, On-resistance $R_D$

![[Pasted image 20250803185808.png]]

1. MOSFET can be modelled as an ideal switch + on-resistance (resistance when conducting)
2. Diode can be modelled as a voltage drop + resistance. From a V-I curve (in datasheet), we approximate it using a straight line that's valid over the intended operating range. The intercept voltage would be $V_D$ and the slope is $1/R-D$ 

# Circuit Analysis

As usual we break down the circuit into 2 subintervals:

![[Pasted image 20250803184734.png]]

Average inductor voltage and capacitor current waveform:

![[Pasted image 20250803185943.png]]

DC components (average) of inductor voltage and capacitor current:
$$
\begin{align}
\langle v_L \rangle &= D(V_g - IR_L - IR_{on}) + D'(V_g - IR_L - V_D - IR_{D} - V) = 0 \\
\langle i_C \rangle &= D(-V/R) + D'(I - V/R) = 0
\end{align}
$$
# Equivalent circuits

Simplifying the equations, we can construct a circuit